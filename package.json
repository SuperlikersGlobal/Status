{
  "name": "superlikers-status-v0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Status v0 — lightweight Jira-like work view backed by GitHub Issues.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
