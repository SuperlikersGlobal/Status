# Token with access to the private Status repository.
# Keep this value only on the server. Never expose it in public/app.js.
GITHUB_TOKEN=github_pat_xxx
GITHUB_REPO=SuperlikersGlobal/Status
PORT=4173
